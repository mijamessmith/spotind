import React, { useContext } from 'react';



export default async function TrackSearch() {
    const [track, setNewTrack] = useContext("4WhyHQ2BXi2VU1iaFbF6jv")

    getNewTrack()

    return (
        <div>
            <button onClick={() => this.getNewTrack()}>Get a new track</button>
        </div>
        )
}